# Results

Generated experiment outputs belong here locally, but most are ignored by Git by default to avoid committing large or noisy artifacts.

For final project reporting, selectively version lightweight, publication-ready outputs such as summary tables or figures when useful.

Each reported result should be traceable to an experiment configuration, dataset split manifest, and model/checkpoint identifier.
